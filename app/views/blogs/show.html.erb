<h1><%= @blog.title %></h1>
<p><%= @blog.content %></p>
<%= button_to 'Delete', @blog, method: :delete, data: { confirm: 'Are you sure?' }, class: 'delete-button' %>
<%= link_to 'Edit', edit_blog_path(@blog) %> |
<%= link_to 'Back', blogs_path %>

